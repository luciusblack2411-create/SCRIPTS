name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, "chore/repository-skeleton-v0.1"]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project
        run: python -m pip install -e '.[dev]'

      - name: Verify dependencies
        run: python -m pip check

      - name: Diagnose import sorting
        run: |
          ruff check --select I --fix src/cisco_assessment/devtools/pr_review/scope.py
          git diff -- src/cisco_assessment/devtools/pr_review/scope.py
          exit 1

      - name: Type check
        run: mypy src/cisco_assessment

      - name: Tests
        run: pytest

      - name: Build package
        run: python -m build

      - name: CLI smoke test
        run: cisco-assessment version
