# Genie interface-status spike only.
#
# Smallest top-level set observed to be necessary for a normally resolved,
# supportable runtime:
# - genie provides the Genie core plus genie.libs.parser;
# - pyats provides the pyATS runtime imported by genie.metaparser.
#
# Do not use --no-deps here. The spike intentionally measures the complete
# resolver-selected footprint before any later dependency optimization.
genie==26.6
pyats==26.6
