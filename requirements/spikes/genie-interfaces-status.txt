# Genie interface-status spike only.
#
# Use the smallest single top-level Cisco distribution that provides the Genie
# core plus the parser library through normal dependency resolution. Do not use
# --no-deps here: this spike intentionally measures the officially resolvable
# dependency footprint before attempting later optimization/isolation.
genie==26.6
